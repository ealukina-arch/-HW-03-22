# Новостной портал на Django

Проект новостного портала с системой подписок и email уведомлениями.

## 🚀 Возможности

### 📰 Основной функционал
- Создание новостей и статей
- Регистрация и аутентификация пользователей
- Комментирование публикаций
- Рейтинговая система для постов и комментариев
- Поиск и фильтрация новостей
- Разделение на категории

### 🔔 Система подписок (НОВОЕ)
- **Подписка на категории** - пользователи могут подписываться на интересующие категории
- **Email уведомления** - автоматическая отправка уведомлений о новых постах
- **Управление подписками** - страница "Мои подписки" для управления подписками
- **HTML письма** - красивые email шаблоны с превью контента

## 🛠 Технологии

- **Backend**: Django 4.2, Django Allauth
- **Database**: SQLite
- **Frontend**: HTML, CSS, Bootstrap
- **Email**: Django SMTP, консольный вывод для разработки
- **Authentication**: Django Allauth (email + социальные сети)

## 📋 Модели данных

### Основные модели
- `User` - пользователи (стандартная Django модель)
- `Author` - авторы публикаций
- `Category` - категории новостей
- `Post` - публикации (новости/статьи)
- `Comment` - комментарии пользователей

### Модели подписок (НОВОЕ)
- `Subscription` - подписки пользователей на категории
- Связь Many-to-Many между User и Category через Subscription

## 🔐 Система прав

- **Обычные пользователи** - чтение, комментирование, подписки
- **Авторы** - создание и редактирование публикаций
- **Администраторы** - полный доступ через админку

## ✉️ Email уведомления

### При создании нового поста:
- ✅ **Тема письма**: заголовок статьи
- ✅ **Содержание**: HTML с заголовком и первыми 50 символами текста
- ✅ **Приветствие**: "Здравствуй, username. Новая статья в твоём любимом разделе!"
- ✅ **Получатели**: все подписчики категорий поста

### Настройки email:
```python
# Для разработки (письма в консоль)
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'

# Для продакшена
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.yandex.ru'
EMAIL_PORT = 587
EMAIL_USE_TLS = True



# Django News Portal

Полнофункциональный новостной портал на Django с системой аутентификации, группами и правами.

## Функциональность

- ✅ Аутентификация через email и социальные сети (Google - Временно не работает, Yandex)
- ✅ Система групп: common и authors
- ✅ Автоматическое добавление в группу common при регистрации
- ✅ Кнопка "Стать автором" для получения прав
- ✅ CRUD операции для новостей и статей
- ✅ Поиск и фильтрация новостей
- ✅ Постраничный вывод
- ✅ Кастомный фильтр цензуры

## Установка

1. Клонируйте репозиторий:

git clone https://github.com/ваш-username/django-news-portal.git
cd django-news-portal

2.Установите зависимости:
pip install -r requirements.txt

3.Выполните миграции:
python manage.py migrate

4.Создайте суперпользователя:
python manage.py createsuperuser

5.Запустите сервер:
python manage.py runserver