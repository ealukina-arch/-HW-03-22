# 🗞️ News Portal

Современная новостная платформа с системой подписок и автоматическими рассылками. Проект реализует полнофункциональное веб-приложение для публикации новостей с асинхронными уведомлениями и периодическими дайджестами.

## 📁 Репозиторий проекта

```bash
# Основной репозиторий
https://github.com/Dark-Master-Web/Module22.git

# Клонирование проекта
git clone https://github.com/Dark-Master-Web/Module22.git
cd Module22
✨ Особенности
📧 Автоматические рассылки - мгновенные уведомления и еженедельные дайджесты

🔔 Система подписок - категоризированные подписки по интересам

⚡ Асинхронные задачи - фоновая обработка с Celery и Redis

👥 Управление ролями - авторы, читатели, администраторы

📱 Адаптивный дизайн - работает на всех устройствах

🔍 Поиск и фильтрация - удобный поиск по контенту

🏗️ Архитектура
text
Django App → Celery → Redis → Email
    ↓           ↓        ↓       ↓
PostgreSQL   Workers   Broker  Subscribers
🛠 Технологический стек
Backend
Django 4.2+ - основной фреймворк

Celery - асинхронные задачи

Redis - брокер сообщений и кэш

PostgreSQL - база данных

Frontend
Bootstrap 5 - UI компоненты

JavaScript - интерактивность

Jinja2 - шаблонизация

Дополнительно
Django Celery Beat - планировщик задач

Pillow - работа с изображениями

python-dotenv - переменные окружения

🚀 Быстрый старт
Предварительные требования
Python 3.8+

Redis Server

PostgreSQL (опционально)

Установка
Клонирование репозитория

bash
git clone https://github.com/Dark-Master-Web/Module22.git
cd Module22
Создание виртуального окружения

bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
Установка зависимостей

bash
pip install -r requirements.txt
Настройка окружения

bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
Миграции базы данных

bash
python manage.py migrate
Создание суперпользователя

bash
python manage.py createsuperuser
Загрузка тестовых данных

bash
python manage.py loaddata fixtures/categories.json
python manage.py loaddata fixtures/users.json
Настройка Git (опционально)
Если вы хотите добавить свой репозиторий:

bash
# Добавление удаленного репозитория
git remote add origin https://github.com/your-username/your-repo.git

# Проверка удаленных репозиториев
git remote -v

# Первая загрузка кода
git push -u origin main
Запуск для разработки
Запуск Redis (новое окно терминала)

bash
redis-server
Запуск Celery Worker (новое окно терминала)

bash
celery -A NewsPortal worker --loglevel=info --pool=solo
Запуск Celery Beat (новое окно терминала)

bash
celery -A NewsPortal beat --loglevel=info
Запуск Django сервера

bash
python manage.py runserver
Откройте в браузере

text
http://localhost:8000
📧 Настройка email
Для тестирования рассылок настройте SMTP в файле .env:

ini
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@yandex.ru
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@yandex.ru
Или для разработки используйте консольный бэкенд:

ini
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
🗃️ Модели данных
Основные сущности
Пользователи - авторы и читатели с ролями

Категории - тематические разделы новостей

Подписки - связь пользователей с категориями

Публикации - новости и статьи

Уведомления - история рассылок

🔄 Задачи Celery
Немедленные уведомления
python
# При создании новости подписчики получают уведомление
send_immediate_notification_task.delay(post_id)
Еженедельные дайджесты
python
# Каждый понедельник в 8:00
app.conf.beat_schedule = {
    'weekly-newsletter': {
        'task': 'news.tasks.send_weekly_newsletter',
        'schedule': crontab(hour=8, minute=0, day_of_week=1),
    },
}
🧪 Тестирование
bash
# Запуск всех тестов
python manage.py test

# Запуск тестов с покрытием
coverage run manage.py test
coverage report

# Запуск конкретного приложения
python manage.py test news --verbosity=2
🐳 Docker развертывание
Использование Docker Compose
bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
Сервисы в Docker
web - Django приложение

celery - воркеры для задач

celery_beat - планировщик задач

redis - брокер сообщений

postgres - база данных

📊 Администрирование
Панель администратора
text
http://localhost:8000/admin
Мониторинг Celery
bash
# Статус воркеров
celery -A NewsPortal inspect active

# Статистика
celery -A NewsPortal inspect stats

# Цветы мониторинг (если установлен)
celery -A NewsPortal flower
🔧 Команды управления
bash
# Создание фикстур
python manage.py dumpdata news.Category --indent=2 > fixtures/categories.json

# Очистка кэша
python manage.py clear_cache

# Проверка системы
python manage.py check --deploy

# Сбор статических файлов
python manage.py collectstatic
🔄 Управление версиями
Работа с Git
bash
# Проверка статуса
git status

# Добавление изменений
git add .

# Коммит изменений
git commit -m "Описание изменений"

# Отправка в репозиторий
git push origin main

# Получение обновлений
git pull origin main
🌐 Production развертывание
Рекомендуемые настройки
База данных: PostgreSQL

Брокер: Redis с персистентностью

Веб-сервер: Gunicorn + Nginx

Статические файлы: CDN или WhiteNoise

Медиа файлы: Cloud Storage (S3)

Переменные окружения для production
ini
DEBUG=False
ALLOWED_HOSTS=your-domain.com,www.your-domain.com
SECRET_KEY=your-production-secret-key
DATABASE_URL=postgres://user:password@host:port/database
REDIS_URL=redis://redis:6379/0
🤝 Разработка
Структура проекта
text
Module22/
├── news/                 # Основное приложение
│   ├── models.py        # Модели данных
│   ├── tasks.py         # Задачи Celery
│   ├── signals.py       # Сигналы Django
│   └── templates/       # Шаблоны
├── NewsPortal/          # Настройки проекта
│   ├── settings/        # Настройки (dev/prod)
│   ├── celery.py        # Конфигурация Celery
│   └── urls.py          # Маршруты
├── fixtures/            # Тестовые данные
├── requirements.txt     # Зависимости
└── manage.py           # Утилита управления
Code Style
bash
# Проверка стиля
flake8 .

# Форматирование
black .
isort .
📈 Мониторинг и логи
Логирование задач
python
# В tasks.py
logger.info(f"Notification sent to {count} users")
logger.error(f"Failed to send email: {error}")
Просмотр логов
bash
# Логи Django
tail -f logs/django.log

# Логи Celery
tail -f logs/celery.log

# Логи Redis
redis-cli monitor
🐛 Устранение неисправностей
Common Issues
Redis не запущен

bash
redis-server
Celery задачи не выполняются

bash
celery -A NewsPortal purge  # Очистка очереди
Миграции не применены

bash
python manage.py migrate
Статические файлы не загружаются

bash
python manage.py collectstatic
Проблемы с Git репозиторием

bash
# Проверка подключения
git remote -v

# Сброс репозитория (осторожно!)
git reset --hard origin/main